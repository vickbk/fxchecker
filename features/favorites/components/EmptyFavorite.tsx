import { EmptySection } from "@/shared/utils";

export const EmptyFavorite = () => {
  return (
    <EmptySection
      heading="No pinned Pairs yet"
      text="Pin a pair to track it's rate here. Tap the star icon on the conversion or comparison row"
    />
  );
};
